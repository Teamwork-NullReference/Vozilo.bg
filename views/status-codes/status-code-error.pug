extends ../_layout

block body-content
    .container.text-center
        .col-md-12
            h1 
                = result.code
            h2 Bad request
            p 
                = result.err